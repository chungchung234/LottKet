<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="User">
    <select id="getUser" resultType="lotte.com.lottket.dto.UserDto">
        select user_id, user_name, user_profile_image, user_address, user_detail_address, user_grade, user_role, user_gender, user_age, user_birthday, user_email
        from user
        WHERE user_id = #{userId}
    </select>

    <select id="findByUserId" parameterType="long" resultType="lotte.com.lottket.dto.UserDto">
        select *
        from user
        where userid = #{userId}
    </select>
    
    <select id="signIn" parameterType="lotte.com.lottket.dto.UserDto">
        SELECT * FROM user
        WHERE user_id=#{userId}
    </select>

    <insert id="signUp" parameterType="lotte.com.lottket.dto.UserDto">
        INSERT INTO user(user_id, user_name, user_profile_image, user_address, user_detail_address, user_grade, user_role, user_gender, user_age, user_birthday, user_email)
        VALUES(#{userId}, #{userName}, #{user_profileImage}, #{userAddress}, #{userDetailAddress}, #{userGrade}, #{userRole}, #{userGender}, #{userAge}, #{userBirthday}, #{userEmail})
    </insert>

</mapper>